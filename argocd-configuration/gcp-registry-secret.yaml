kubectl create secret docker-registry gcp-registry-secret -n homolog \
  --docker-server=https://gcr.io \
  --docker-username=_json_key \
  --docker-password="$(cat pipeline-fastech-homolog-77c18f206391.json)" \
  --docker-email=dev@local \
  --dry-run=client -o yaml > gcp-registry-secret.yaml